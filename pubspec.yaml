name: modular_app
description: Modular App.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  bloc: ^8.0.2
  flutter_bloc: ^8.1.3
  hydrated_bloc: ^9.1.2
  path_provider: any
  flutter_modular: ^6.3.2
  safe_device: ^1.1.7
  features_home_presentation:
    path: "features/home/presentation"
  features_weather_domain:
    path: "features/weather/domain"
  features_weather_data:
    path: "features/weather/data"
  features_weather_presentation:
    path: "features/weather/presentation"
  features_reddit_posts_domain:
    path: "features/reddit_posts/domain"
  features_reddit_posts_data:
    path: "features/reddit_posts/data"
  features_reddit_posts_presentation:
    path: "features/reddit_posts/presentation"
  features_forms_presentation:
    path: "features/forms/presentation"
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true