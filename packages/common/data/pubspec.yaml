name: common_data
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  dio_cache_interceptor: ^3.5.0
  flutter_secure_storage: ^9.2.2
  common_domain:

dev_dependencies:
  flutter_lints: ^5.0.0

flutter: