resolution: workspace
name: features_experiments_presentation
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_modular: ^6.4.1
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  shaders:
    - shaders/rainbow.frag